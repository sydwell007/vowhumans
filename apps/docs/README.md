# Documentation application boundary

The canonical Markdown documentation currently lives in `/docs`. This boundary is reserved for a searchable documentation site once public API stability warrants a separate deployment.

