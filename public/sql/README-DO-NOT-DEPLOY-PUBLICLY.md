# Operator warning

This is a sanitized transfer folder, not website content. Import these files through the protected Afrihost database tool and then remove them from any web-accessible host directory. Vercel excludes this folder through `.vercelignore`.
