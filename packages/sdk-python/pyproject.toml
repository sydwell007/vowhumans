[project]
name = "vowhumans"
version = "0.1.0"
description = "Server-side Python SDK for VowHumans"
requires-python = ">=3.11"
license = "UNLICENSED"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

