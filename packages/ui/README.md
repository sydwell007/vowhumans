# UI package

Shared consent badges, session states and media disclosure components move here as additional web surfaces are added. The Studio currently keeps them local to preserve a small Phase 1 dependency graph.

