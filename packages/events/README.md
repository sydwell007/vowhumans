# Events package

Canonical event envelope: UUID event ID, organisation ID, event type, schema version, ISO timestamp, synthetic/resource references, and HMAC signature metadata. Private transcript text is excluded from integration events.

