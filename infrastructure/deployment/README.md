# Deployment artefacts

See `docs/DEPLOYMENT.md`. Keep Studio/CPU, realtime and GPU workloads independently scalable. Vercel is suitable for the Next.js control plane only; LiveKit agents, FFmpeg and GPU inference belong on long-running container infrastructure.

